export interface Expense {
	createdAt: Date;
	name: string;
	price: string;
	category: string;
	note: string;
	id: string;
}

export interface Income {
	createdAt: Date;
	name: string;
	price: string;
	category: string;
	note: string;
	id: string;
}
